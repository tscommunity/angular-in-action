import {WaterfallDirective} from "./waterfall.directive";

describe("WaterfallDirective", () => {
  it("should create an instance", () => {
    const directive = new WaterfallDirective();
    expect(directive).toBeTruthy();
  });
});
